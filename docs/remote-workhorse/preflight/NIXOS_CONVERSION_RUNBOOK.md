# NixOS Conversion Runbook

Updated: 2026-05-03

Status: `NIXOS_BOOT_VERIFIED`

This runbook prepares the destructive conversion from the proven Ubuntu base
host to NixOS. The conversion must not run without explicit action-time
confirmation.

## Current Proven State

| Field | Value |
| --- | --- |
| Droplet ID | `568689911` |
| Host | `24.144.113.25` |
| Source image | `Ubuntu 24.04.3 LTS` |
| Live OS | `NixOS 25.11 (Xantusia)` |
| NixOS version | `25.11.10031.755f5aa91337` |
| Snapshot ID | `227115138` |
| Snapshot name | `windburn-workhorse-nyc1-base-20260503-0830Z` |
| Snapshot created | `2026-05-03T08:30:44Z` |

## Upstream Pins

`nixos-infect` is pinned before execution:

| Field | Value |
| --- | --- |
| Repository | `https://github.com/elitak/nixos-infect` |
| Commit | `40f62a680bb0e8f2f607d79abfaaecd99d59401c` |
| Commit date | `2026-03-22T21:54:35Z` |
| Script SHA-256 | `4354bd68773b41da65c0e815202c43c8549713b3ed3ff6381c71fbc0b0a840ab` |
| Channel | `nixos-25.11` |
| Channel revision at preflight | `755f5aa91337890c432639c60b6064bb7fe67769` |

Primary references:

- `nixos-infect` README says DigitalOcean can run with
  `PROVIDER=digitalocean`, warns that a completed run wipes the target root
  filesystem, and lists Ubuntu 24.04 x64 as a successful DigitalOcean source.
- NixOS manual documents in-place install using `NIXOS_LUSTRATE`; it warns that
  the old distribution becomes non-bootable if boot/config is wrong.

## Local Files

- Host import: `nixos/hosts/windburn-workhorse-nyc1/windburn-workhorse.nix`
- Guarded runner: `scripts/nixos-conversion.sh`

The host import is intentionally conservative for first boot: SSH policy, root
and `windburn` authorized keys, firewall port 22, flakes, trusted wheel users,
and basic build/operator packages. It deliberately does not override boot,
hardware, network, hostname, or `system.stateVersion`; those are generated by
the pinned `nixos-infect` script during first boot.

## Dry Run

```sh
scripts/nixos-conversion.sh
```

Dry-run verifies:

- Droplet inventory.
- Snapshot inventory.
- SSH host-key fingerprints.
- Strict SSH read-only OS/Nix state.
- Pinned `nixos-infect` download SHA-256.

Latest dry-run:

```text
mode=dry-run
host=24.144.113.25
droplet_id=568689911
snapshot_id=227115138
nix_channel=nixos-25.11
nixos_infect_commit=40f62a680bb0e8f2f607d79abfaaecd99d59401c
nixos_infect_sha256=4354bd68773b41da65c0e815202c43c8549713b3ed3ff6381c71fbc0b0a840ab
remote_os=Ubuntu 24.04.3 LTS
remote_nix=nix_bin=absent, nixos_rebuild=absent, nix_root=absent
result=remote host was not modified
```

## Apply Gate

The staging command has completed successfully:

```sh
scripts/nixos-conversion.sh \
  --apply \
  --confirm-destructive-nixos-conversion \
  --confirm-snapshot-id 227115138
```

This runs `nixos-infect` with `NO_REBOOT=1`, so the install is staged and logs
are captured before the reboot that triggers lustration.

Stage proof:

- `stage_complete=1`
- `/etc/NIXOS=present`
- `/etc/NIXOS_LUSTRATE=present`
- `/nix/var/nix/profiles/system=present`
- System profile:
  `/nix/store/ld2nxfda2l2yjnrgdq5q5nx33bb6c0yx-nixos-system-windburn-workhorse-nyc1-25.11.10031.755f5aa91337`
- The host is still running Ubuntu 24.04.3 on Linux 6.8.0, so the lustrate
  reboot has not happened.

Full proof: `docs/remote-workhorse/preflight/NIXOS_STAGE_PROOF.md`

## Lustrate Reboot Gate

The reboot command was reboot-only. It must be used instead of rerunning
`scripts/nixos-conversion.sh` because the install is already staged.

Dry-run:

```sh
scripts/nixos-lustrate-reboot.sh
```

Apply:

```sh
scripts/nixos-lustrate-reboot.sh \
  --apply \
  --confirm-lustrate-reboot \
  --confirm-snapshot-id 227115138
```

The script proves the staged state first, triggers a DigitalOcean reboot only
after explicit confirmation, waits for SSH to return, verifies `ID=nixos`, and
records whether SSH host keys changed.

Result:

- DigitalOcean reboot action `3168332769` completed.
- SSH returned on `24.144.113.25`.
- Live OS reports `NixOS 25.11 (Xantusia)`.
- Kernel is `Linux 6.12.84 x86_64 GNU/Linux`.
- `systemctl is-system-running` reports `running`.
- `sshd` is `active`.
- `/etc/NIXOS_LUSTRATE` is absent after boot.
- `nix` and `nixos-rebuild` are present in `/run/current-system/sw/bin`.

Full proof: `docs/remote-workhorse/preflight/NIXOS_BOOT_PROOF.md`

After success, `scripts/nixos-lustrate-reboot.sh` is idempotent: it detects
`remote_state=already_nixos` and exits without triggering another reboot.

## Recovery

If the host does not return after reboot:

1. Use DigitalOcean console or action logs to inspect boot state.
2. Restore Droplet `568689911` from snapshot `227115138`, or create a new
   Droplet from that snapshot.
3. Remove stale local `known_hosts` only after capturing the replacement host
   key with `ssh-keyscan`.

## Stop Conditions

Return `BLOCK` before apply if:

- Snapshot `227115138` is missing.
- Strict SSH proof fails.
- The downloaded `nixos-infect` SHA-256 does not match the pinned value.
- The operator has not confirmed destructive conversion.
- There is no clear path to observe the reboot and verify NixOS identity.
